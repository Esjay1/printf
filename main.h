#ifndef MAIN_H
#define MAIN_H
#include <unistd.h>
#include <stdarg.h>
#include <stdlib.h>
#include <string.h>

int specifier(const char *spec, ...);
int _printf(const char *format, ...);
char ampsamp(void);





#endif
